# About Freezing Specified Layers in Layout Viewports

A major benefit to using layout viewports is that you can selectively freeze layers in each layout viewport. You can also
specify default visibility settings for new viewports and for new layers. As a result, you can view different objects in each
layout viewport.

You can freeze or thaw layers in current and future layout viewports without affecting other viewports. Frozen layers are
invisible. They are not regenerated or plotted. In the illustration, the layer showing terrain has been frozen in one viewport.

![](../images/GUID-5D1AB223-394D-4DA5-86A5-034EAD431B65-low.png)

Thawing the layer restores visibility. The easiest way to freeze or thaw layers in the current viewport is to use the Layers
palette.

In the Layers palette, on the right side, use the column labeled VP Freeze to freeze one or more layers in the current layout
viewport. To display the VP Freeze column, you must be on a layout. Specify the current layout viewport by double-clicking
anywhere within its borders.

You can create new layers that are frozen in all existing and new layout viewports. Then you can thaw the layers in the viewports
you specify. This is a shortcut for creating a new layer that is visible only in a single viewport.

## Freeze or Thaw Layers Automatically in New Layout Viewports

You can set visibility defaults for specific layers in all new layout viewports. For example, you can restrict the display
of dimensions by freezing the DIMENSIONS layer in all new viewports. If you create a viewport that requires dimensions, you
can override the default setting by changing the setting in the current viewport. Changing the default for new viewports does
not affect existing viewports.

## Create New Layers That Are Frozen in All Layout Viewports

You can create new layers that are frozen in all existing and new layout viewports. Then you can thaw the layers in the viewports
you specify. This is a shortcut for creating a new layer that is visible only in a single viewport.

#### Related Information

* [To Freeze or Thaw Layers Automatically in New Layout Viewports](To-Freeze-or-Thaw-Layers-Automatically-in-New-Layout-Viewports.md)
* [To Freeze or Thaw Layers in the Current Layout Viewport](To-Freeze-or-Thaw-Layers-in-the-Current-Layout-Viewport.md)
* [To View a List of Layers that are Frozen in the Current Viewport](To-View-a-List-of-Layers-that-are-Frozen-in-the-Current-Viewport.md)

[Please send us your comment about this page](..)

[![Creative Commons License](../images/ccLink.png)](http://creativecommons.org/licenses/by-nc-sa/3.0/) *Except where otherwise noted, this work is licensed under a [Creative Commons Attribution-NonCommercial-ShareAlike 3.0 Unported License](http://creativecommons.org/licenses/by-nc-sa/3.0/). Please see the [Autodesk Creative Commons FAQ](http://autodesk.com/creativecommons) for more information.*